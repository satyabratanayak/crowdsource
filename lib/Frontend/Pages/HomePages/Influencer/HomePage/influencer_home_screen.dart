import 'package:crowdsource/Utilities/import.dart';

class InfluencerHomeScreen extends StatelessWidget {
  const InfluencerHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Influencer Home",
        style: kStylePrimaryBold,
      ),
    );
  }
}
